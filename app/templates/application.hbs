{{!-- The following component displays Ember's default welcome message. --}}
{{!-- Feel free to remove this! --}}

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">

    </div>

    <div class="collapse navbar-collapse" id="menu-collapse">
      <ul class="nav navbar-nav" >
        <li>{{ link-to 'Home' 'index' }}</li>

        <li >{{ link-to 'Pricing' 'pricing' }}</li>

        <li>{{ link-to 'Contact' 'contact' }}</li>


          {{#if localStorage.currentUser }}
            <li>Welcome, {{#link-to 'account'}} {{localStorage.currentUser}} {{/link-to}} <button {{action 'logout'}}>Logout</button></li>
          {{else}}
            <li>{{ link-to 'Login' 'login' }}</li>
          {{/if}}

      </ul>



    </div>
  </div>
</nav>

<div class="container">
  {{outlet}}
</div>
